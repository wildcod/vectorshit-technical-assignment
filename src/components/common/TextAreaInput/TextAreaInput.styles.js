const styles = {
  container: {
    '& .textAreaInput': {
      resize: 'none',
      borderColor: 'grey.medium',
      borderRadius: '4px',
      outlineColor: 'primary.main'
    }
  }
};

export default styles;
